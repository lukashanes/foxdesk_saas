{
    "version": "0.3.129",
    "date": "2026-06-03",
    "min_php": "8.1",
    "changelog": [
        "SaaS migration API now accepts token-protected self-hosted sync plans, table chunks, and attachment uploads",
        "Platform operators can manage migration bridge tokens from the workspace detail view",
        "Self-hosted migration data is mapped into the target tenant workspace with source object mapping",
        "Ticket creation no longer sends duplicate internal emails when an admin or agent assigns a ticket to themselves",
        "Ticket search now looks across all non-archived tickets by default instead of only the current view"
    ],
    "delete_files": []
}
